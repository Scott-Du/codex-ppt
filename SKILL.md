---
name: codex-ppt
description: "Codex PPT skill for image-first PowerPoint decks with built-in image_gen. 当用户要求做 PPT、幻灯片、演示文稿、presentation、deck、slides 时使用。必须逐页调用 Codex 图像生成工具生图并流式展示进度，最后只负责把生成图组装成 pptx。"
---

# Codex PPT 制作

核心不变量：每一页正式幻灯片 = 一张 Codex 内置 `image_gen` 生成的完整图片。PPTX 只是装配容器，不负责设计和绘制页面。

## 产物目录

每个 PPT 任务必须在用户所制定的位置或用户当前所在的工作目录之下，找到合适的位置，创建独立工作文件夹，所有过程文件和最终文件都放在这个文件夹内。

默认结构：

```text
<PPT名>-YYYYMMDD-HHMM/
  outline.md
  style-preview.png
  <PPT名>.pptx
  images/
    01.png
    02.png
```

- 工作文件夹优先放在用户明确指定的目录下。
- 如果用户只给了项目目录，则先判断项目中是否已有最相关的业务子目录，并在该子目录下创建工作文件夹；只有找不到更合适位置时，才放在当前工作目录一级子文件夹。
- 创建工作文件夹前，先告诉用户拟使用的工作文件夹绝对路径。
- 文件夹名使用用户确认的 PPT 文件名加时间戳，例如 `年度经营复盘-20260516-1430`。
- `outline.md` 是分页大纲的唯一源文件。确认后的大纲必须写入这里；后续修改大纲时也必须更新这个文件，并以它作为执行阶段的唯一依据。
- `style-preview.png` 是统一风格阶段生成的正文第一页效果图，必须保存后用绝对路径展示给用户预览。
- `images/` 只存放逐页生成的正式幻灯片图片，按 `01.png`、`02.png` 顺序命名。
- 最终 PPTX 直接放在工作文件夹根目录。

## 硬性约束

- 环境检查只做一件事：确认当前处于 Codex 环境且内置 `image_gen` 工具可用；满足即执行，否则暂停并说明本 skill 需要 Codex 图像生成环境。无需查找本地 `image2` 命令。
- 执行阶段必须逐页调用 `image_gen`；每一页正式幻灯片对应一次独立生图调用和一个保存的图片文件。
- 不得用本地脚本、PIL/canvas、HTML/CSS/SVG、PowerPoint 形状、截图拼接、模板渲染、现有图片改字等方式替代正式页生图。
- 代码只可用于创建文件夹、复制或重命名图像生成工具返回的图片、检查尺寸和页数、把已生成图片逐页铺满组装成 pptx。
- 可使用 `scripts/assemble_image_ppt.py` 做最终机械装配和校验；该脚本不得替代正式页生图，不得绘制页面正文内容。
- Image 2 模型的能力很强，不用考虑生图中文字是否清晰的问题。

## 流程

收集 → 确认→ 执行 → 交付

### 收集

读取用户素材和要求。关键信息缺失时一次性补问，用户选择跳过或不给出答案时，则使用合理默认值进入下一步。

在进入确认阶段前，准备好安全的 PPT 文件名；第 1 步确认完成后，立即创建工作文件夹和 `images/` 子文件夹。

### 确认：三步，逐步推进，每步确认后才进下一步

**第 1 步：PPT 元信息**

标题、语言、页数、目标读者、使用场景、页面比例、输出形式。

用户确认后：

1. 创建工作文件夹：`/<PPT名>-YYYYMMDD-HHMM/`。
2. 创建图片子文件夹：`/<PPT名>-YYYYMMDD-HHMM/images/`。
3. 告知用户本次任务的工作文件夹绝对路径。

**第 2 步：统一风格设定**

同时输出：

- **统一风格提示词**——用自然语言描述整套 PPT 的视觉风格，须覆盖：整体气质、主色调与配色、字体风格与层级、留白与信息密度、图形语言、图标风格、版式倾向
- **正文第一页效果图**——调用图像生成工具，用该提示词生成正文第一页（非封面）作为风格样张

效果图必须保存为工作文件夹下的 `style-preview.png`，并用绝对路径展示给用户预览。用户可以看着效果图改提示词，也可以直接说感受（如"太商务了"），改到满意为止。

**第 3 步：分页大纲**

每页三个字段：页码、页面标题、页面内容与设计。

"页面内容与设计"用自然语言同时描述这一页讲什么和怎么呈现（信息重点、视觉组织方式、图示建议等）。这段描述在执行阶段会与统一风格提示词组合，直接用于生图。

分页大纲只写 PPT 页面需要展示的内容，使用正向、对外可读的表达。避免把内部提醒、防御性说明、风控备注写进页面内容。生成 `outline.md` 后，检查中文“不”字以及“不要、不能、不可、避免”等表达，逐条思考是否能改成“已完成什么、聚焦什么、适合什么、计划如何验证”等正向描述；少量不可替代的客观表达可以保留。

大纲必须写入工作文件夹下的 `outline.md`，并保持以下固定格式：

```markdown
# <PPT标题> 分页大纲

## 元信息

- 标题：
- 语言：
- 页数：
- 目标读者：
- 使用场景：
- 页面比例：
- 输出形式：

## 统一风格提示词

<最终确认的统一风格提示词>

## 分页大纲

### 第 01 页：<页面标题>

页面内容与设计：
<自然语言描述这一页讲什么和怎么呈现>

### 第 02 页：<页面标题>

页面内容与设计：
<自然语言描述这一页讲什么和怎么呈现>
```

后续如用户要求调整页数、标题、页面顺序或单页内容，必须先更新 `outline.md`，再继续执行。

### 执行

逐页循环生图。每页生成指令 = `outline.md` 中的统一风格提示词 + 该页的页面内容与设计。

每一页都按以下顺序执行，不得跳步：

1. 组合该页的完整图像生成提示词。
2. 调用 Codex 内置 `image_gen`。
3. 从图像生成工具返回路径或 `$CODEX_HOME/generated_images/...` 中取得本次生成图片；如果需要从默认目录识别文件，以调用后新生成的图片为准。
4. 将该图片复制到工作文件夹下的 `images/` 子文件夹中，按 `01.png`、`02.png` 顺序命名。
5. 立即用绝对路径把该页图片发给用户看，并输出 `第 N/M 页已完成`。
6. 用户不打断时，才进入下一页。
- 只有当前页图片已由图像生成工具真实返回后，才可以继续下一页。
- 不得先用脚本批量绘制占位图，也不得把“逐页生图”留到交付前补做或自查。
- 若用户打断、暂停、提出修改当前页或退回上一阶段的请求，则遵从用户指令。

全部生成完毕后，使用 `scripts/assemble_image_ppt.py` 或等价的机械装配流程，将 `images/` 下所有图片按顺序组装为 pptx。

推荐调用方式：

```bash
python3 <skill-dir>/scripts/assemble_image_ppt.py --workdir <工作文件夹绝对路径> --title <PPT文件名> --expected-pages <页数> --ratio 16:9
```

### 单页重做

用户要求重做某一页时，按以下顺序处理：

1. 先更新 `outline.md` 中对应页的页面标题或页面内容与设计。
2. 创建 `images/archive/`。
3. 将旧图移动到 `images/archive/NN-YYYYMMDD-HHMM.png`。
4. 重新调用图像生成工具生成该页，并保存为 `images/NN.png`。
5. 用绝对路径展示新图，输出 `第 NN 页已重做`。
6. 重新装配 PPTX。
7. 交付时说明重做页码、旧图归档路径、新图路径和新 PPTX 路径。

### 交付

交付前做一致性核对：

- 图片数量等于确认页数。
- 每个正式页图片均来自逐页图像生成调用。
- PPTX 每页仅铺满插入对应图片，不额外绘制正文内容。
- `outline.md`、`style-preview.png`、`images/` 和最终 PPTX 都在同一个工作文件夹下。

如果发现任何正式页不是由图像生成工具生成，该 PPT 不合格，必须从该页重新执行“逐页循环生图”。

交付时必须给出工作文件夹、最终 PPTX 和 `outline.md` 的绝对路径，并说明逐页图片在 `images/`。交付 pptx 后，用户可对任意一页单独重做、替换、重新组装。
